{
  "name": "spudcast",
  "version": "0.0.0",
  "private": true,
  "description": "Retro linear TV streaming platform — turn your media library into always-on channels.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "concurrently -n backend,admin,player -c blue,green,magenta \"pnpm --filter @spudcast/backend dev\" \"pnpm --filter @spudcast/admin dev\" \"pnpm --filter @spudcast/player dev\"",
    "build": "pnpm --filter @spudcast/admin build && pnpm --filter @spudcast/player build",
    "start": "pnpm --filter @spudcast/backend start",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm --filter @spudcast/backend test"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["argon2", "better-sqlite3", "esbuild"]
  }
}
